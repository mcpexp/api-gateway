# API Gateway

This is an API gateway project.

## Health Endpoint

A new health check endpoint has been added via the `healthcheck.py` file. The `health()` function returns a JSON response `{"status": "ok"}` to indicate the service is healthy.